<%= form_with(model: vehicle, local: true) do |form| %>
  <% if vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

      <ul>
      <% vehicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :plate %>
    <%= form.text_field :plate, id: :vehicle_plate %>
  </div>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand, id: :vehicle_brand %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.number_field :model, id: :vehicle_model %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value, id: :vehicle_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
